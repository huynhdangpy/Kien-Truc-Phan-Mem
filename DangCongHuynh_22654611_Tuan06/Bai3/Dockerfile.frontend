# Frontend Dockerfile - Serve static files with basic HTTP server
FROM node:18-alpine

WORKDIR /app

# Copy frontend files
COPY frontend/ .

# Install simple HTTP server
RUN npm install -g http-server

# Expose port
EXPOSE 3000

# Start simple HTTP server
CMD ["http-server", "-p", "3000", "-c-1"]
